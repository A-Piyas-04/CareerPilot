fastapi
uvicorn[standard]
python-dotenv
supabase
pydantic
pydantic-settings
python-multipart
asyncpg>=0.29
sqlalchemy[asyncio]>=2.0
alembic>=1.13
pgvector>=0.2.5
pypdf
python-docx
scikit-learn
numpy
anthropic
# sentence-transformers is NOT installed by default — it pulls in PyTorch + CUDA (~2 GB).
# Only install it when you want semantic embeddings:
#   pip install sentence-transformers
# Then set EMBEDDING_BACKEND=transformers in backend/.env
